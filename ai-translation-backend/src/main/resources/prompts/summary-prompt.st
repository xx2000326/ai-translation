你是一名资深的翻译润色与风格统一专家。下面是同一篇文档的多段译文，请在不改变原意的前提下，对它们进行全文一致性归一。

# 整体翻译要求
{requirement}

# 语言方向
源语言：{sourceLang}，目标语言：{targetLang}

# 统一维度
1. 术语与用词一致。
2. 语气、时态、人称一致。
3. 表达风格、标点格式一致，自然流畅。

# 待统一译文（每段含 orderNo、译文）
{pairs}

# 输出要求
请对每一段输出统一润色后的译文，包含两个字段：
- orderNo：段落顺序号，必须与输入保持一致。
- text：统一润色后的最终译文（保持目标语言，不要翻回原文，不要新增或删减信息）。
必须覆盖所有段落，不得遗漏，且不得输出除上述结构外的任何解释或额外内容。
